import 'package:flutter/material.dart';

class ChantCell extends StatelessWidget {
  const ChantCell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
